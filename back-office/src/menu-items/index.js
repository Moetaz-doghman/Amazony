// project import
import dashboard from './dashboard';
import gallery from './gallery';
import produit from './produit';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, produit, gallery]
};

export default menuItems;
